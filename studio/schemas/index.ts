import { article } from './article';
import { siteSettings } from './siteSettings';

// Export all schemas — add new document types here as the CMS grows
export const schemaTypes = [siteSettings, article];
